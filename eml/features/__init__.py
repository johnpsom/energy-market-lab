"""Layer 4: the feature engine.

Turns raw warehouse rows into a model-ready, hourly-indexed feature matrix. See
docs/FEATURES.md for the catalog and the leakage rule. Entry point: `features.build.build_matrix`.
"""
